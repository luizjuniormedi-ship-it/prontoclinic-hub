name: Deploy Auth Admin Contract to VPS

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: "Commit SHA aprovado para publicação"
        required: true
      bootstrap_helper:
        description: "Atualizar helper privilegiado antes da auditoria"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: prontomedic-auth-admin-production
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.commit_sha }}
          fetch-depth: 0

      - name: Validate approved commit
        env:
          COMMIT_SHA: ${{ inputs.commit_sha }}
        run: |
          [[ "$COMMIT_SHA" =~ ^[0-9a-f]{40}$ ]] || {
            echo "::error::commit_sha deve ser um SHA-1 completo"
            exit 1
          }
          test "$(git rev-parse HEAD)" = "$COMMIT_SHA"
          git fetch origin main --no-tags
          git merge-base --is-ancestor "$COMMIT_SHA" origin/main || {
            echo "::error::A publicação aceita apenas commits já integrados em main"
            exit 1
          }

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run type-check
      - run: npm run test:unit
      - run: npm run build
      - name: Validate Auth backend contract
        run: |
          node --check local-auth-server.mjs
          node --check local-auth-admin.mjs
          node deploy/auth-service/verify-contract.mjs
      - name: Type check Edge Functions
        run: |
          docker run --rm \
            -v "$PWD:/app" -w /app \
            --entrypoint deno denoland/deno:2.5.6 \
            check \
            supabase/functions/auth-admin/index.ts \
            supabase/functions/dicom-bridge/index.ts \
            supabase/functions/telemedicina-daily/index.ts
      - name: Test Edge Function security helpers
        run: |
          docker run --rm \
            -v "$PWD:/app" -w /app \
            --entrypoint deno denoland/deno:2.5.6 \
            test --allow-env supabase/functions/_shared/cors_test.ts
      - name: Test Edge runtime deployment contract
        run: bash deploy/edge-runtime/deploy-functions.test.sh
      - name: Test coordinated Auth deployment contract
        run: |
          bash -n deploy/auth-service/deploy-atomic.sh
          bash -n deploy/auth-service/build-auth-artifact.sh
          node deploy/auth-service/verify-atomic-deploy.mjs
      - run: |
          if grep -R '"Access-Control-Allow-Origin": "\*"' \
            supabase/functions/auth-admin \
            supabase/functions/dicom-bridge \
            supabase/functions/telemedicina-daily; then
            echo "::error::CORS wildcard encontrado nas Edge Functions"
            exit 1
          fi
      - name: Build immutable release artifacts
        env:
          COMMIT_SHA: ${{ inputs.commit_sha }}
        run: |
          install -d release
          bash deploy/auth-service/build-auth-artifact.sh "$COMMIT_SHA" "$PWD" "$PWD/release"
          tar -czf "release/edge-${COMMIT_SHA}.tgz" \
            supabase/functions/_shared \
            supabase/functions/auth-admin \
            supabase/functions/dicom-bridge \
            supabase/functions/telemedicina-daily \
            deploy/edge-runtime
          (cd release && sha256sum "edge-${COMMIT_SHA}.tgz" > "edge-${COMMIT_SHA}.tgz.sha256")
          cp supabase/migrations/20260802183000_local_auth_admin_contract.sql \
            "release/migration-${COMMIT_SHA}.sql"
          install -d release/bootstrap-root/deploy/auth-service release/bootstrap-root/deploy/edge-runtime
          cp deploy/auth-service/deploy-atomic.sh \
            deploy/auth-service/install-coordinator.sh \
            deploy/auth-service/ecosystem.config.cjs \
            release/bootstrap-root/deploy/auth-service/
          cp deploy/edge-runtime/deploy-functions.sh \
            release/bootstrap-root/deploy/edge-runtime/
      - uses: actions/upload-artifact@v4
        with:
          name: auth-admin-release-${{ inputs.commit_sha }}
          if-no-files-found: error
          path: release/*

  deploy:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: Production
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: auth-admin-release-${{ inputs.commit_sha }}
          path: release

      - name: Validate transport secrets
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          VPS_KNOWN_HOSTS: ${{ vars.VPS_KNOWN_HOSTS_PUBLIC }}
        run: |
          for variable in VPS_HOST VPS_USER VPS_SSH_PRIVATE_KEY VPS_KNOWN_HOSTS; do
            test -n "${!variable}" || {
              echo "::error::Secret obrigatório ausente: ${variable}"
              exit 1
            }
          done

      - name: Configure SSH
        env:
          VPS_SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          VPS_KNOWN_HOSTS: ${{ vars.VPS_KNOWN_HOSTS_PUBLIC }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "$VPS_SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf '%s\n' "$VPS_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Configure root bootstrap identity
        if: ${{ inputs.bootstrap_helper }}
        env:
          VPS_ROOT_SSH_PRIVATE_KEY: ${{ secrets.VPS_ROOT_SSH_PRIVATE_KEY }}
        run: |
          test -n "$VPS_ROOT_SSH_PRIVATE_KEY" || {
            echo "::error::Secret obrigatório ausente: VPS_ROOT_SSH_PRIVATE_KEY"
            exit 1
          }
          printf '%s\n' "$VPS_ROOT_SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519_root
          chmod 600 ~/.ssh/id_ed25519_root

      - name: Bootstrap privileged deploy helper
        if: ${{ inputs.bootstrap_helper }}
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
        run: |
          trap 'rm -f ~/.ssh/id_ed25519_root' EXIT
          helper="release/bootstrap-root/deploy/edge-runtime/deploy-functions.sh"
          test -s "$helper"
          bash -n "$helper"
          (
            cd "$(dirname "$helper")"
            sha256sum "$(basename "$helper")"
          ) > /tmp/prontomedic-edge-deploy.sha256
          scp -i ~/.ssh/id_ed25519_root -o IdentitiesOnly=yes \
            "$helper" /tmp/prontomedic-edge-deploy.sha256 \
            "root@${VPS_HOST}:/tmp/"
          ssh -i ~/.ssh/id_ed25519_root -o IdentitiesOnly=yes \
            "root@${VPS_HOST}" 'bash -s' <<'EOF'
          set -Eeuo pipefail
          helper="/tmp/deploy-functions.sh"
          checksum="/tmp/prontomedic-edge-deploy.sha256"
          target="/usr/local/sbin/prontomedic-edge-deploy"
          backup_dir="/var/backups/prontomedic/edge-helper"
          stamp="$(date -u +%Y%m%dT%H%M%SZ)"

          cd /tmp
          sha256sum -c "$(basename "$checksum")"
          bash -n "$helper"
          install -d -o root -g root -m 0700 "$backup_dir"
          if test -f "$target"; then
            cp -a "$target" "$backup_dir/prontomedic-edge-deploy-${stamp}"
          fi
          install -o root -g root -m 0750 "$helper" "${target}.next"
          bash -n "${target}.next"
          mv -Tf "${target}.next" "$target"
          rm -f "$helper" "$checksum"
          echo "EDGE_HELPER_BOOTSTRAP_PASS"
          EOF

          scp -r -i ~/.ssh/id_ed25519_root -o IdentitiesOnly=yes \
            release/bootstrap-root "root@${VPS_HOST}:/tmp/prontomedic-auth-bootstrap"
          ssh -i ~/.ssh/id_ed25519_root -o IdentitiesOnly=yes \
            "root@${VPS_HOST}" \
            "bash /tmp/prontomedic-auth-bootstrap/deploy/auth-service/install-coordinator.sh /tmp/prontomedic-auth-bootstrap /opt/prontomedic/backend"

      - name: Audit privileged deployment contracts
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          ssh "${VPS_USER}@${VPS_HOST}" \
            "sudo /usr/local/sbin/prontomedic-edge-deploy --audit-contract && sudo /usr/local/sbin/prontomedic-auth-deploy audit"
      - name: Upload immutable release
        env:
          COMMIT_SHA: ${{ inputs.commit_sha }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          scp \
            "release/auth-${COMMIT_SHA}.tgz" \
            "release/auth-${COMMIT_SHA}.tgz.sha256" \
            "release/edge-${COMMIT_SHA}.tgz" \
            "release/edge-${COMMIT_SHA}.tgz.sha256" \
            "release/migration-${COMMIT_SHA}.sql" \
            "${VPS_USER}@${VPS_HOST}:/tmp/"

      - name: Deploy atomically and smoke test
        env:
          COMMIT_SHA: ${{ inputs.commit_sha }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          PUBLIC_URL: https://prontomedic.191-252-196-6.sslip.io
        run: |
          ssh "${VPS_USER}@${VPS_HOST}" \
            "sudo /usr/local/sbin/prontomedic-auth-deploy preflight '$COMMIT_SHA' '/tmp/auth-${COMMIT_SHA}.tgz' '/tmp/auth-${COMMIT_SHA}.tgz.sha256' '/tmp/edge-${COMMIT_SHA}.tgz' '/tmp/edge-${COMMIT_SHA}.tgz.sha256' '/tmp/migration-${COMMIT_SHA}.sql'"
          ssh "${VPS_USER}@${VPS_HOST}" \
            "sudo /usr/local/sbin/prontomedic-auth-deploy deploy '$COMMIT_SHA' '/tmp/auth-${COMMIT_SHA}.tgz' '/tmp/auth-${COMMIT_SHA}.tgz.sha256' '/tmp/edge-${COMMIT_SHA}.tgz' '/tmp/edge-${COMMIT_SHA}.tgz.sha256' '/tmp/migration-${COMMIT_SHA}.sql'"

          smoke_failed=0
          for function_name in auth-admin dicom-bridge telemedicina-daily; do
            status="$(curl -sS --connect-timeout 5 --max-time 15 \
              -D /tmp/headers -o /tmp/response -w '%{http_code}' \
              -X OPTIONS \
              -H 'Origin: https://prontomedic.191-252-196-6.sslip.io' \
              "${PUBLIC_URL}/functions/v1/${function_name}")"
            grep -Fqi 'Access-Control-Allow-Origin: https://prontomedic.191-252-196-6.sslip.io' /tmp/headers || status="cors-${status}"
            test "$status" = "200" || {
              echo "::error::${function_name} falhou no preflight CORS: ${status}"
              cat /tmp/response
              smoke_failed=1
            }
          done

          if test "$smoke_failed" -ne 0; then
            ssh "${VPS_USER}@${VPS_HOST}" \
              "sudo /usr/local/sbin/prontomedic-auth-deploy rollback"
            exit 1
          fi
